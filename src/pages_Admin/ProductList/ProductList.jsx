import React from 'react';

export async function loader() {
    return {};
}

const ProductList = () => {
    return (
        <div>
            ProductList
        </div>
    );
};

export default ProductList;